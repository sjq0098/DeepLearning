
# 实验报告写作 Skill

## 一、核心目标

一份好的实验报告，是对一项已完成技术工作的完整交代。

它需要让读者高效理解六件事：

1. 为什么要做这个实验；
2. 完成实验需要理解哪些背景知识；
3. 实验实际上是怎样完成的；
4. 最终得到了什么结果；
5. 过程中遇到了什么问题，又是怎样排查和解决的；
6. 通过实验获得了什么认识。

实验报告不应该像零散笔记、截图集合、代码粘贴或指导书复述。它应该是一条受控的技术推理链。

论文写作和实验报告写作有相通之处：二者都需要明确动机、清晰结构、准确术语、证据支撑和章节衔接。但二者的中心任务不同。论文强调“创新性”：提出了什么新方法、新理论、新系统或新发现。实验报告强调“完成度与理解度”：读者需要相信实验确实完成了，作者理解了背后的机制，结果经过验证，问题排查不是表面化操作。

但有问题需要注意，实验报告中需要保留讨论的空间不overclaim，明确结论的条件边界等而不是完全声明结论，这是不严谨不负责任的行为


## 二、默认报告结构

除非用户提供了其他格式要求，实验报告默认采用以下六段式结构：

```text
实验目的：为什么要做这个实验？                 动机
    ↓
实验内容 / 实验原理：完成实验需要理解什么？    知识
    ↓
实验步骤 / 实现过程：实际怎样完成？            过程
    ↓
实验结果：最终达到什么效果？                  终态
    ↓
遇到的问题与解决方法：哪里出过错，如何排查？  弯路
    ↓
实验总结：回头看获得了什么认识？              反思
```

每一章都应该回答一个问题，同时为下一章制造自然需求。不要把各章写成互不相关的容器。

一个有效的自检方法是：把上一章最后一段和下一章第一段连起来读。如果语义上不能自然衔接，说明中间缺少过渡句或过渡段。

## 三、章节级组织规则

### 1. 实验目的

实验目的应该解释“为什么要做这个实验”，而不是简单复述实验题目或指导书要求，但是一个最重要的原则是，不能错误传达实验指导书中的要求。

较弱写法：

```text
本实验要求实现一个简单的指令执行框架。
```

较好写法：

```text
在一个计算机系统中，程序从静态的机器指令变成动态的执行行为，依赖取指、译码、执行和状态更新等环节。本实验的目的，是在已有框架的基础上补全关键执行逻辑，使抽象的指令语义能够转化为可验证的机器状态变化，从而理解处理器执行模型和软件模拟器之间的对应关系。
```

对于机器学习或深度学习实验，也不能只写“训练一个分类模型”。应该说明这个实验要揭示什么能力或概念，例如特征表示、优化过程、泛化能力、正则化、模型容量、消融实验、鲁棒性或评价方法。

### 2. 实验内容 / 实验原理

这一章回答的问题是：在进入实现之前，需要向读者展示作者理解了哪些知识？

一个好的实验内容的介绍，需要合理的表述一种对实验目的的拓展，能比较好的组织内容介绍本次实验中涉及到的背景知识，又能比较清晰的阐述本次实验要做什么方便衔接下一步实验步骤中对编码和实现思想的阐述。

其内部逻辑通常应遵循：

```text
当前系统 / 当前任务缺什么
    ↓
这个缺口为什么重要
    ↓
需要哪些概念、机制、算法或方法来补足
    ↓
这些概念之间如何配合
    ↓
补足之后能够完成什么
```

对于计算机系统类实验，本章可以解释指令周期、存储层次、MMIO、特权级、中断、文件系统、进程调度、同步机制、调用约定等。

对于机器学习 / 深度学习实验，本章可以解释数据集假设、特征构造、模型结构、损失函数、优化器、评价指标、训练集 / 验证集 / 测试集划分、过拟合、正则化或消融设计。

本章原则上不贴实现代码。它讲“是什么”和“为什么需要”，不是讲“具体写了哪些代码”。

### 3. 实验步骤 / 实现过程

这一章回答的问题是：实验实际上是怎样完成的？

每个主要步骤都应该构成一个小型技术闭环：

```text
动机：为什么需要做这一步？
实现：具体改了什么、配置了什么、训练了什么？
验证：如何确认这一步有效？
承接：验证结果如何引出下一步？
```

不要机械罗列“步骤一、步骤二、步骤三”。每一步都应该有明确动机和验证方式。

对于实现型报告，只贴有解释价值的代码。代码块前后必须说明为什么这样实现，而不是让代码自己承担解释任务。避免粘贴大段能直接从代码表面读懂、但没有设计含义的内容。

对于机器学习 / 深度学习报告，实验步骤应包含足够的可复现信息，例如数据划分方式、预处理方法、模型配置、超参数、训练轮数、优化器、学习率、评价指标、硬件或运行环境约束，以及必要时的随机种子。

### 4. 实验结果

这一章回答的问题是：最终达到了什么状态？有什么证据证明？

实验结果不能只写“运行成功”，也不能只放截图。报告需要把观察到的结果与实验目的明确对应起来。

常见证据包括：

- 终端输出、测试通过日志、benchmark 结果、生成文件、可视化截图；
- accuracy、loss、F1、AUC、perplexity、BLEU、延迟、吞吐、内存占用等指标；
- baseline 与改进版本的对比；
- 正确预测样例、错误样例、生成结果或可视化输出；
- 能说明系统或模型边界的失败案例。

对于机器学习 / 深度学习报告，必须区分训练集指标、验证集指标和测试集指标。只有训练损失下降，并不能证明模型具有泛化能力。

### 5. 遇到的问题与解决方法

这一章回答的问题是：实验过程中哪里出错了？怎样定位、解释和修复？

每个问题都应该写成一个完整的排查故事：

```text
现象：观察到了什么异常？
定位：通过什么检查缩小范围？
根因：真正的问题是什么？
修复：如何修改？
验证：修复后如何确认问题消失？
```

不要只写最后的修复方法。这个章节的价值在于展示排查路径，而不仅是展示正确答案。

如果有多个问题，可以按时间线、依赖关系或抽象层次排序。例如：

```text
编译 / 环境问题
    ↓
运行时逻辑问题
    ↓
性能 / 精度 / 稳定性问题
    ↓
边界情况或工程化问题
```

对于机器学习 / 深度学习报告，常见问题包括维度不匹配、数据泄漏、loss 不稳定、梯度爆炸或消失、过拟合、类别不平衡、标签映射错误、训练和推理预处理不一致、评价指标使用错误等。

### 6. 实验总结

这一章回答的问题是：通过实验获得了什么可迁移的认识？

总结不应只是重复实验结果。它应从实验过程、机制理解、工程实现、模型训练、评价方法或问题排查中提炼认识。

较弱写法：

```text
通过本实验，我掌握了模型训练流程，实验结果较好。
```

较好写法：

```text
本实验表明，模型性能不仅取决于网络结构本身，也强烈依赖数据划分、归一化方式和评价指标的选择。训练集准确率的提升并不必然意味着泛化能力提升，因此实验中必须同时观察验证集指标，并结合错误样本分析判断模型是否真正学到了有效特征。
```

系统类报告应总结机制层面的理解；人工智能类报告应总结建模、优化、评价、数据处理或误差分析层面的理解。

## 四、段落级组织规则

每一段都应该有一个主句和若干支撑句。

主句说明本段的核心判断。支撑句解释为什么成立、怎样实现、证据是什么，或者这个判断为什么重要。

如果一段中出现两个彼此独立的论点，应拆成两段。

因果关系必须显式写出来。不要让读者自行推断两个句子为什么相邻。

较弱写法：

```text
使用位域表示标志位。中间加入匿名字段。
```

较好写法：

```text
使用位域表示标志位，是因为条件跳转只需要访问单个标志位，而部分指令又需要整体更新状态寄存器。中间加入匿名字段，是为了保证各标志位落在体系结构手册规定的 bit 位置上，避免结构体布局与硬件语义不一致。
```

技术细节要回答“为什么”，不只是说明“是什么”。

较弱写法：

```text
对输入图像进行了归一化处理。
```

较好写法：

```text
对输入图像进行归一化处理，是为了使不同像素通道的数值范围保持一致，减少优化过程中由尺度差异造成的梯度震荡。该处理也使训练阶段和测试阶段的数据分布更加一致，从而降低评估结果受预处理差异影响的风险。
```

## 五、跨章节内容映射

同一个技术概念可以在多个章节出现，但不同章节的处理角度必须不同。

基本规则是：

```text
实验内容：讲是什么、为什么需要
实验步骤：讲怎么实现、怎么配置、怎么训练、怎么验证
实验结果：讲最终证据
遇到的问题：讲哪里做错过、如何排查、如何修复
实验总结：讲抽象认识
```

不要在多个章节重复同一段解释。如果某个概念必须再次出现，用一句交叉引用即可，例如：

```text
如实验内容中所述，EFLAGS 是条件跳转读取状态信息的核心结构；本节重点说明其实现在代码层面如何完成。
```

```text
具体实现见 3.2 节，本节只分析该实现对最终测试结果的影响。
```

常见概念映射示例：

| 概念 | 实验内容怎么讲 | 实验步骤怎么讲 | 遇到的问题怎么讲 | 实验结果怎么讲 |
|---|---|---|---|---|
| EFLAGS / 状态寄存器 | 解释标志位作用、条件跳转为什么依赖它 | 说明如何表示、初始化、更新标志位 | 说明某个标志位计算错误如何导致跳转异常 | 用测试通过或程序正确执行证明语义链路有效 |
| MMIO / 设备访问 | 解释内存编址 I/O 与端口 I/O 的区别 | 说明如何在读写路径中分流设备地址 | 说明黑屏、无输出或设备无响应的排查过程 | 用截图、日志或设备状态证明 I/O 链路打通 |
| CNN / 卷积网络 | 解释局部感受野、参数共享、池化和特征层级 | 说明模型结构、输入尺寸和训练配置 | 说明维度错误、过拟合或收敛异常的定位 | 用验证集指标、曲线和样本预测证明训练效果 |
| Transformer / 注意力机制 | 解释自注意力、位置编码、mask 或多头机制 | 说明模型搭建、tokenization 和训练超参 | 说明显存溢出、mask 错误、loss 不下降等问题 | 用指标、生成样例或消融对比证明效果 |
| 评价指标 | 解释指标衡量什么以及为何适合任务 | 说明如何计算、在哪个数据集上计算 | 说明指标异常是否由标签、阈值或数据泄漏导致 | 用表格或图展示最终指标 |

## 六、图、表、代码的使用原则

图表和代码都是用于方便的展示不便使用文字描述的实验内容实验思想或者思路，用图的目的是减少文字叙述减少篇幅，但有一个大忌是光出现图表又没有与之对应的文字解释。

### 1. 图的使用

图应该展示文字难以高效表达的信息，而不是把已经说清楚的文字重新排版。

适合使用图的内容包括：

- 指令周期流程图；
- 系统架构图；
- 模型结构图；
- 数据预处理流程图；
- 训练 / 验证 loss 曲线；
- 混淆矩阵；
- 生成图像、分割结果、检测结果；
- UI、设备显示或程序运行截图。

图应该放在它所支撑的论述附近。除非格式要求，不要把所有图统一堆到报告末尾。

### 2. 表的使用

表适合呈现结构化对比信息，例如：

- 实验指标；
- 超参数配置；
- 消融实验结果；
- 指令分类；
- 标志位定义；
- 模块职责；
- 不同方法的优缺点对比。

表不适合承载完整排查过程。排查过程有时间顺序和因果链，通常用叙述性段落更清楚。

### 3. 代码的使用

代码通常只应该出现在“实验步骤 / 实现过程”中。

代码块应至少满足以下条件之一：

- 展示核心实现；
- 体现非显然的设计选择；
- 解释接口、数据结构或算法；
- 在问题排查中对比错误版本与正确版本。

不要粘贴模板代码、整文件代码或没有解释的框架代码。每段代码都应该回答“为什么要这样写”。

## 七、归因与人称

### 1. 归因要准确

框架提供的能力归因于框架，手册规定归因于手册，指导书要求归因于指导书，自己的实现和排查过程则用中性的实施性表述。

示例：

```text
框架已经提供了数据加载接口，因此实验重点不在文件解析，而在模型结构与训练过程的设计。
```

```text
根据体系结构手册，条件跳转需要读取状态标志位，因此算术指令对标志位的更新会直接影响控制流。
```

```text
指导书要求比较不同正则化策略，因此实验中保持模型结构不变，仅改变 dropout 和权重衰减配置。
```

### 2. 人称要克制

实验报告是学术技术文体，不是个人博客。大多数技术描述应尽量让技术对象作主语，而不是频繁使用“我”。

较弱写法：

```text
我在模型里加入了 Dropout，然后我发现验证集准确率提高了。
```

较好写法：

```text
模型中加入 Dropout 后，训练集与验证集之间的准确率差距缩小，说明随机失活在一定程度上缓解了过拟合。
```

第一人称并非绝对禁止。当确实需要表达主观判断、权衡或反思时，可以使用“我认为”。

示例：

```text
我认为该实现的主要优势在于接口边界清晰，但代价是调试时需要同时检查多个模块之间的状态传递。
```

## 八、实验报告与论文写作的区别

实验报告可以借鉴论文写作的严谨性，但不能机械模仿论文的创新性叙述。

二者共通之处：

- 都需要明确动机；
- 都需要逻辑清晰的结构；
- 都需要证据支撑而不是空泛判断；
- 都需要精确定义和一致术语；
- 都需要服务于论证的图表；
- 都需要章节之间的自然过渡。

二者差异如下：

| 维度 | 论文 | 实验报告 |
|---|---|---|
| 中心任务 | 证明创新性和学术贡献 | 证明完成度、理解度、验证过程和反思深度 |
| 核心问题 | 提出了什么新知识、新方法或新系统 | 做了什么，为什么可行，如何证明完成 |
| 方法部分 | 支持复现，并服务于创新论证 | 展示实现路径、实验纪律和验证方式 |
| 结果部分 | 通常需要与已有方法或 baseline 比较 | 需要证明实验目标已经达成 |
| 讨论部分 | 解释贡献、局限和未来工作 | 反思机制理解、调试过程和可迁移经验 |
| 语气 | 研究贡献导向 | 技术交代导向 |

不要把普通课程实验强行写成研究创新。实验报告应借鉴论文的清晰、严谨和证据意识，而不是借用论文式的夸大贡献。

## 九、修改检查清单

修改或诊断实验报告时，按以下维度检查。

### 1. 结构

- 每一章是否回答了不同的问题？
- 每一章是否自然引出下一章？
- 报告是否是一条推理链，而不是无关材料的拼接？
- 实验目的、实验原理、实现过程、实验结果、问题排查和实验总结是否边界清楚？

### 2. 内容

- 实验目的是否解释了动机，而不是复述题目？
- 实验内容是否解释概念，而不是提前贴代码？
- 实验步骤是否说明了每一步为什么必要？
- 实验结果是否提供了证据，而不是只声称成功？
- 问题排查是否包含现象、定位、根因、修复和验证？
- 实验总结是否提炼了可迁移认识？

### 3. 技术质量

- 技术术语是否前后一致？
- 因果关系是否显式？
- 关键设计选择是否得到解释？
- 重要结论是否有日志、指标、截图、曲线、测试或样例支撑？
- 涉及数据、模型或训练时，是否说明数据划分、超参数和评价协议？

### 4. 文体

- 每段是否围绕一个主句展开？
- 第一人称是否克制而必要？
- 归因是否准确？
- 图、表、代码是否放在支撑它们的论述附近？
- 重复内容是否被删除，并改为交叉引用？
